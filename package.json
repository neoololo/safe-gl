{
  "name": "safe-gl",
  "version": "1.0.0",
  "description": "",
  "main": "./dist/SafeGL.js",
  "types": "./dist/SafeGL.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepare": "cd import-glsl && pnpm i && tsc --project . && cd .. && tsc --project ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "esbuild": "^0.23.1",
    "typescript": "^5.5.4"
  },
  "devDependencies": {
    "patch-package": "^8.0.0"
  }
}
